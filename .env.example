# Microsoft Fabric Warehouse Configuration
# Copy this file to .env and fill in your actual credentials

# Fabric Warehouse SQL Endpoint (without https://)
# Example: your-workspace-name.datawarehouse.fabric.microsoft.com
FABRIC_SERVER=your-workspace.datawarehouse.fabric.microsoft.com

# Database name in your Fabric Warehouse
FABRIC_DATABASE=your_database_name

# Authentication Method: Choose one of the following:
# 1. "CLI" - Uses Azure CLI login (run 'az login' first)
# 2. "INTERACTIVE" - Uses interactive browser authentication
# 3. "SERVICE_PRINCIPAL" - Uses service principal (requires CLIENT_ID, CLIENT_SECRET, TENANT_ID)
AUTH_METHOD=CLI

# Service Principal credentials (only needed if AUTH_METHOD=SERVICE_PRINCIPAL)
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here
AZURE_TENANT_ID=your-tenant-id-here

# Data Generation Settings
# BATCH_SIZE: How many rows to GENERATE per cycle (per table)
# Recommended: 10000-50000 rows for good performance
BATCH_SIZE=10000

# CHUNK_SIZE: How many rows to INSERT per SQL statement
# Recommended: 500-2000 (larger = faster but more memory)
CHUNK_SIZE=1000

# BATCH_INTERVAL: Seconds to wait between cycles (use 0 for continuous)
BATCH_INTERVAL=5
